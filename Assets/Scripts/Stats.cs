using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stats : MonoBehaviour
{
    /*public float maxHealth;
    public float health;
    public float attackSpd;
    public float attackTime;
    public float attackDmg;
    WeaponAtk weaponAtkScript;
    // Start is called before the first frame update
    void Start()
    {
        weaponAtkScript = GameObject.FindGameObjectWithTag("Player").GetComponent<WeaponAtk>();
    }

    // Update is called once per frame
    void Update()
    {
        if (health <= 0)
        {
            Destroy(gameObject);
            //weaponAtkScript.targetedEnemy = null;
            weaponAtkScript.performMeleeAttack = false;
        }
    }*/
}
